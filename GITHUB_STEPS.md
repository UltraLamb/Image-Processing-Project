# GitHub Upload Steps

1. Create a new GitHub repository named `carracing-v3-ppo-ray-features`.
2. Upload everything inside `FINAL_SUBMISSION/github_repo/`.
3. Replace this placeholder everywhere after upload:

https://github.com/UltraLamb/CarRacingV3-PPO-RayFeatures

Optional command-line path:

```bash
cd FINAL_SUBMISSION/github_repo
git init
git add .
git commit -m "Final CarRacing PPO project submission"
git branch -M main
git remote add origin https://github.com/UltraLamb/CarRacingV3-PPO-RayFeatures.git
git push -u origin main
```
