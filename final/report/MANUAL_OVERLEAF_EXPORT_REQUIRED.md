# MANUAL_OVERLEAF_EXPORT_REQUIRED

The report source is ready, but this checkout does not have a local LaTeX
toolchain available. Do not treat a locally patched PDF as the official final
report.

Use:

`final/overleaf/Image_Processing_Project_Overleaf_Package.zip`

Steps:

1. Upload the ZIP to Overleaf.
2. Compile with pdfLaTeX.
3. Confirm the report contains the corrected `Failure modes` paragraph.
4. Download the PDF.
5. Replace `final/report/Image_Processing_Project_V2_IEEE_Report.pdf`.
6. Re-run the final text extraction checks.

The archived DOCX and PDF preview are kept only for traceability under
`archive/stale_editable_material/`.
